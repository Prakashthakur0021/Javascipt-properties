/* 
includes :- using the array includes method you can find if an element 
            is included inside an array or not(return true or false)
*/
console.clear();

const fruits = ["apple", "mango", "grapes"];

console.log(fruits.includes("apple"));

console.log(fruits.includes("mango") ? "Yuppy!" : "Oops");
